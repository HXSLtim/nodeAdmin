/**
 * Default tenant ID used when no tenant context is available.
 * TODO: Replace with TenantContext injection once multi-tenant middleware is in place.
 */
export const DEFAULT_TENANT_ID = 'default';
